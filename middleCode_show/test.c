void fun(int i){
    print_int(i);
}
int main()
{
    int li[1][2];
    int a = 1, b = 2, a;
    int c = 2;

    for (int i = 0; i < 10; i = i + 1) {
        int i =10;
        if (a < 5) a = a + 1;
        else if (a >= 5) {
            a = a + 2;
        }
    }
    // 或者是其他自定义的输出函数
    print_int(i);
    while (a < 40) {
        a = a + 1;
    }
    print_int(a);
    return 0;
}